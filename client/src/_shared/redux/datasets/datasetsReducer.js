/**
 * @param state
 * @param action.type
 * @param action.payload
 * @returns {Array.<Datasets>}
 */
const datasetsReducer = (state, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default datasetsReducer;
